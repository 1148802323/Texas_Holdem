# 版本推进日志

本项目尚未发布可联机使用的版本。以下版本号用于记录开发里程碑；Git 标签会在需要正式发布时创建。

## v0.6.0 — 2026-09-25：手机竖版牌桌与庄家按钮

- 手机牌桌改成纵向椭圆，缩小座位、公共牌与底池；常见手机宽度下所有座位无需横向滑动即可显示。
- 首手从已入座且有筹码的玩家中随机抽取庄位，此后按实体座位轮转。
- 在庄家座位前方显示独立的 D 按钮；昵称不附加庄位字样。
- 补充庄位抽取和轮转测试，并在 390×844、320×568 手机视口验证九座牌桌完整显示。

## v0.5.0 — 2026-09-25：旁观、换座与椭圆牌桌

- 邀请进入时只需昵称；空座位上的加号可入座，站起后可选择新座位，筹码始终与当前身份绑定。
- 椭圆牌桌围绕公共牌展示座位和总底池；左侧显示昵称盈亏，右侧搜索自己的手牌记录。
- 退出会兑出剩余筹码、失效当前凭证；再次使用同名昵称会获得新身份，不能读取旧身份的私人历史。正在参与本手时禁止换座、站起和退出。
- 增加数据库 v2 迁移、旁观权限与座位变更测试；验证桌面和手机牌桌布局。

## v0.4.0 — 2026-09-25：浏览器牌桌与实时接口

- 新增只有管理员凭密码可操作的建房页面，以及邀请、入座、牌桌和个人记录界面。
- 提供玩家 Cookie 身份、HTTP 动作接口和按玩家权限发送的 WebSocket 状态更新。
- 每次动作后原子保存下一位玩家的截止时间；30 秒超时自动过牌或弃牌。
- 增加 HTTP、Cookie 和 WebSocket 测试；运行说明见 `docs/web.md`。

## v0.3.0 — 2026-09-25：数据库与买入持久化

- 新增 SQLite 迁移与持久化服务，保存房间、玩家、买入、手牌、动作、结算和私密快照。
- 房间买入上限按买入后的在桌筹码计算；买入与牌局状态变更使用事务和请求 ID 防重复。
- 支持重启恢复、按玩家凭证读取个人视图与历史，并增加 7 项数据库集成测试。
- 提供本地数据库初始化命令；SQLite 数据文件不进入 Git 仓库。

## v0.2.0 — 2026-09-25：逐步操作的牌局引擎

### 新增

- 将单手牌拆分为开始、查询合法动作、提交动作、自动推进及结算；保留原本地模拟入口。
- 为玩家分配唯一 ID，并加入手牌 ID、阶段、行动者、待行动集合、下注状态、截止时间和状态版本。
- 提供服务端专用的私密快照导出与恢复，以及按玩家身份生成的可见状态；未公开的其他玩家底牌和牌堆不会进入玩家视图。
- 行动记录包含实际投入筹码；补充 9 项有断言的规则测试。

### 修正

- 非法操作改为明确拒绝，保持牌局状态不变；拒绝过期版本的重复请求。
- 修复只剩一名玩家能够行动时，仍需由其回应下注的情况。
- 按完整加注及累计短码全下判断加注权，处理不足最小加注的全下。
- 返还未被跟注的筹码；处理多人全下边池、平局余数，并在结算后清空底池、防止重复结算。
- 固定随机种子下连续手牌不再重复同一牌序；无种子时使用系统随机源洗牌。

### 验证与范围

- 在项目 Conda 环境的 Python 3.11.16 下，9 项规则测试及原本地演示通过。
- 目前只有独立游戏引擎；管理员页面、房间、实时通信和数据库尚未实现。房间服务后续需串行处理同桌操作、执行超时，并以事务保存操作和私密快照。

## v0.1.0 — 2026-09-24：项目结构与规则雏形

- 整理原有 Python 卡牌、发牌、牌型评估和模拟对局代码至 `backend/app/engine/`。
- 建立前端、后端、数据库迁移、部署及文档目录；记录首版需求和开发顺序。
- 保留原模拟脚本及说明，完成首次 GitHub 上传。
